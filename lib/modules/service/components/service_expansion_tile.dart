import 'package:flutter/material.dart';

class ServiceExpansionTile extends StatelessWidget {
  const ServiceExpansionTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
